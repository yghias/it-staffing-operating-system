# Observability

- Structured logging with correlation IDs across ingestion, graph loading, API requests, and AI runs.
- Metrics for sync freshness, entity resolution confidence, match acceptance, time-to-fill prediction error, and review queue latency.
- Alerts for failing connectors, schema drift, quality regressions, and degraded ranking performance.
