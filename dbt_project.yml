name: it_staffing_operating_system
version: "1.0.0"
config-version: 2

profile: it_staffing_operating_system

model-paths: ["models"]
seed-paths: ["sample_data/raw"]
macro-paths: ["macros"]
test-paths: ["tests"]

models:
  it_staffing_operating_system:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: table
      +schema: intermediate
    marts:
      +materialized: table
      +schema: mart
