# Fallback Policy

If semantic retrieval, graph traversal, or live feature generation fails:

- revert to rules-based ranking
- annotate the run as degraded
- suppress any claim of confidence parity with normal ranking
