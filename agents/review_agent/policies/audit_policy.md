# Audit Policy

- Every reviewer action must persist actor, timestamp, reason code, and changed fields.
- Reviewer overrides should be sampled weekly for calibration analysis.
- High-impact overrides must be available for staffing leadership audit.
